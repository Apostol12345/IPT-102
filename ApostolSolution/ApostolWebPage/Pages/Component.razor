@page "/try"



<h3>Question</h3>

<!-- Button that triggers the click event -->


<!-- Display message based on the condition -->
<p>1. 1+1=?</p>

<p>A.<button @onclick="OnButtonClick">2</button>@message</p><br />
@code {
    // Variable to track the condition
    private bool isClicked = false;

    // Variable to store the message
    private string message = "";

    // Method that runs when the button is clicked
    private void OnButtonClick()
    {
        // If-else logic
        if (isClicked)
        {
        }
        else
        {
            message = "Correct answer";
            isClicked = true;  // Set the condition to true
        }

    }
}
<p>B.<button @onclick="OnButtonClick1">3</button>@message1</p>
@code {
    // Variable to track the condition
    private bool isClicked1 = false;

    // Variable to store the message
    private string message1 = "";

    // Method that runs when the button is clicked
    private void OnButtonClick1()
    {
        // If-else logic
        if (isClicked1)
        {
        }
        else
        {
            message1 = "Wrong asnwer";
            isClicked1 = true;  // Set the condition to true
        }

    }
}

<p>2. 6+9=?</p>

<p>A.<button @onclick="OnButtonClick3">15</button>@message3</p><br />
@code {
    // Variable to track the condition
    private bool isClicked3 = false;

    // Variable to store the message
    private string message3 = "";

    // Method that runs when the button is clicked
    private void OnButtonClick3()
    {
        // If-else logic
        if (isClicked3)
        {
        }
        else
        {
            message3 = "Correct answer";
            isClicked3 = true;  // Set the condition to true
        }

    }
}
<p>B.<button @onclick="OnButtonClick4">69</button>@message4</p>
@code {
    // Variable to track the condition
    private bool isClicked4 = false;

    // Variable to store the message
    private string message4 = "";

    // Method that runs when the button is clicked
    private void OnButtonClick4()
    {
        // If-else logic
        if (isClicked4)
        {
        }
        else
        {
            message4 = "Wrong asnwer";
            isClicked4 = true;  // Set the condition to true
        }

    }
}
